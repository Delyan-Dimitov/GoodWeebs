@model GoodWeebs.Web.ViewModels.MangaViewModels.MangaListViewModel
@{
    this.ViewData["Title"] = $"All Anime Page";
}
<style>
    .media {
        margin: 0px
    }

    .row {
        margin: 0;
    }
</style>
<div class="row ">
    @foreach (var manga in Model.Mangas)
    {

        <div class="media col-md-4 bg-info text-white border ">
            <img src="@manga.PictureUrl" class="mr-3 img-responsive border-0 width=" 100"">
            <div class="media-body ">
                <a asp-action="Info" asp-route-id="@manga.MangaId" class="text-white">  <h5 class="mt-0">@manga.Title</h5></a>
                <p class="text-white">@(manga.Synposis.Length >= 150 ? manga.Synposis.Substring(0, 150) : manga.Synposis)...</p>

            </div>

        </div>
    }
</div>
<nav aria-label="..." style="margin-top:10px">
    <ul class="pagination justify-content-center ">
        <li class="page-item ">
            <a class="page-link" asp-action="All" asp-route-id="@Model.PreviousPage">Previous</a>
        </li>
        <li class="page-item ">
            <a class="page-link" href="#" tabindex="-1">@Model.PreviousPage</a>
        </li>
        <li class="page-item active">
            <a class="page-link" href="#">@Model.Page <span class="sr-only">(current)</span></a>
        </li>
        <li class="page-item"><a class="page-link" href="#">@Model.NextPage</a></li>
        <li class="page-item">
            <a class="page-link" asp-action="All" asp-route-id="@Model.NextPage">Next</a>
        </li>
    </ul>
</nav>